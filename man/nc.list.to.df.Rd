% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc.list.to.df.R
\name{nc.list.to.df}
\alias{nc.list.to.df}
\title{Convert gridded NetCDF array output to a tidy spatial data frame}
\usage{
nc.list.to.df(
  lat,
  lat.edges,
  lon,
  lon.edges,
  var.arr,
  depth.level,
  dims,
  time.step = NULL
)
}
\arguments{
\item{lat}{Numeric vector of latitude midpoints.}

\item{lat.edges}{Numeric vector of latitude cell edges.}

\item{lon}{Numeric vector of longitude midpoints.}

\item{lon.edges}{Numeric vector of longitude cell edges.}

\item{var.arr}{Array of variable values to use (2D or 3D).}

\item{depth.level}{Integer depth index to extract (3D variables only).}

\item{dims}{Integer number of spatial dimensions (2 or 3).}

\item{time.step}{Integer time index to extract.}
}
\value{
A data frame with columns:
\code{lon.mid}, \code{lon.min}, \code{lon.max},
\code{lat.mid}, \code{lat.min}, \code{lat.max},
and \code{var}.
}
\description{
Internal utility function that converts pre-extracted NetCDF variables
stored as arrays into a long-format data frame with latitude–longitude
midpoints and grid-cell bounds.
}
\details{
The function is model-agnostic and should work for any regularly gridded
lat–lon NetCDF output. Model-specific exceptions (e.g. certain cGENIE
diagnostics with non-standard grids) are handled explicitly where required.

This function expects inputs similar to the outputs of \code{\link{cGENIE.get.nc}} and \code{\link{HADCM3.get.nc}}.

This function does not read NetCDF files directly. It operates on arrays
already extracted from NetCDF output and assumes a regular lat–lon grid.
Special cases for particular model diagnostics (e.g. cGENIE streamfunction
or topography fields) are handled internally.
}
\keyword{internal}
