% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc.df.to.sf.R
\name{nc.df.to.sf}
\alias{nc.df.to.sf}
\title{Convert NetCDF Grid Cell Data Frame to sf Polygons}
\usage{
nc.df.to.sf(df, var.col = "var", crs = 4326)
}
\arguments{
\item{df}{A data frame containing grid-cell bounds. Must include
\code{lon.min}, \code{lon.max}, \code{lat.min}, \code{lat.max}, and at least one data
column (e.g. \code{var}).}

\item{var.col}{Character. Name of the column to attach as polygon
attributes (default \code{"var"}).}

\item{crs}{Integer or character. Coordinate reference system for
the output sf object (default EPSG:4326).}
}
\value{
An \code{sf} object with one polygon per grid cell and attributes
inherited from the input data frame.
}
\description{
Converts a data frame describing gridded NetCDF model output
(with longitude and latitude bounds) into an \code{sf} polygon object
suitable for spatial plotting.
}
\details{
This function is designed to work with the output of
' \code{\link{cGENIE.data}} after cleaning with \code{\link{clean.nc.df}}, but is
model-agnostic and can be used with any model that provides
rectangular grid-cell bounds.

Each row of \code{df} is converted into a rectangular polygon defined by
its longitude and latitude bounds. Attribute data are preserved and
attached to the resulting spatial object.

This function intentionally uses \code{sp} internally for compatibility
with existing cGENIE workflows, but returns a modern \code{sf} object.
}
