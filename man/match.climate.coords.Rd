% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.climate.coords.R
\name{match.climate.coords}
\alias{match.climate.coords}
\title{Match Climate Data to Coordinates (Vectorized, cGENIE/HADCM3)}
\usage{
match.climate.coords(
  clim.dat,
  grid.dat,
  coord.dat,
  lat.name = "p_lat",
  lng.name = "p_lng",
  max_dist = 1000,
  na.method = c("nearest", "same.lat", "keep"),
  verbose = TRUE
)
}
\arguments{
\item{clim.dat}{Data frame with columns \code{lat.mid}, \code{lon.mid}, \code{var}.}

\item{grid.dat}{List from \code{cGENIE.grid()} or equivalent HADCM3 grid function.}

\item{coord.dat}{Data frame with coordinates to match climate data.}

\item{lat.name}{Name of latitude column in coord.dat (default "p_lat").}

\item{lng.name}{Name of longitude column in coord.dat (default "p_lng").}

\item{max_dist}{Maximum allowed distance for matching (km, default 1000 km).}

\item{na.method}{How to handle NA climate values: "nearest" (default), "same.lat", "keep".}

\item{verbose}{Logical; if TRUE, print summary statistics (default TRUE).}
}
\value{
coord.dat with new columns: \code{matched_climate}, \code{lat.bin.mid}, \code{lon.bin.mid}.
}
\description{
Efficiently matches gridded climate data to a set of coordinates, handling NA cells.
}
